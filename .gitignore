# Files ignored because they're binary/unnecessary
.DS_STORE
# Files/Dirs ignored because they're secret/internal only
id_rsa
secrets_repo/
internal_only_repo/

__pycache__/
*.pyc

# Generated DPG assessment / audit outputs (per-repo reports & artifacts, not source)
assessments/
audit-report*.json
dpg-audit-report*.json
dpg-assessment*.json
dpg-nominee*.json
*.assessment.docx
